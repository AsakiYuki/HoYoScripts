import secret from "./secret";

export default class Database {
    async GET(PRIMARY_KEY: string) {}
    async DELETE(PRIMARY_KEY: string) {}
    async PATCH(PRIMARY_KEY: string) {}
    async PUT(PRIMARY_KEY: string) {}
}
